
-	I switched to using 'long long' to store the milliseconds since 1970,
	but it's not required --- a simple long is enough, even if it overflows!

-	memory allocator

-	Scheduler data structure is currently a list
  
		I don't think this is a big problem right now, as only << 100 timer is active at any
		given time, and it only fires if the majority of nodes are *down* or very slow:
		... -> no timer -> OnPrepareTimeout() -> OnProposeTimeout() -> no timer -> ...








- bdb: open must be inside a tx when using tx
- during catchup, the sender does not need to use tx
- the receiver stops the ReplicatedLog and does not serve clients,
  so he only needs atomicity